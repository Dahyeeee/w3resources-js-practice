//Write a JavaScript program to check whether a given string contains only Latin letters and no two uppercase 
//and no two lowercase letters are in adjacent positions.

function check(string){
    if(/[a-z][A-Z]/.test(string))
}